/** Edge-safe auth constants (imported by middleware — keep free of node APIs). */
export const SESSION_COOKIE = "pqt_demo_session";
/** Non-httpOnly hint so the client Header can show "My portal" without a server read. */
export const PORTAL_HINT_COOKIE = "pqt_portal_hint";
